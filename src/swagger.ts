/*import swaggerJsdoc from 'swagger-jsdoc';

const options = {
    definition: {
        openapi: '3.0.0',
        info: {
            title: 'Fuerzas del Cielo API',
            version: '1.0.0'
        },
        servers: [
            {
                url: "http://localhost:3000"
            }
        ],
    },
    apis: ["./routes/*.ts"]
};

const specs = swaggerJsdoc(options);
export default specs; */